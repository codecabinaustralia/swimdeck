<%= form_for @flag do |form| %>
  <% if @flag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flag.errors.count, "error") %> prohibited this flag from being saved:</h2>

      <ul>
      <% @flag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :note, placeholder: "Please describe how you completed this job", rows: 4 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
