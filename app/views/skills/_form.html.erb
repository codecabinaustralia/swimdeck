<%= form_with(model: skill, local: true) do |form| %>
  <% if skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :level_id %>
    <%= form.collection_select(:level_id, Level.all, :id, :title, prompt: true) %>
  </div>

  <div class="field">
    <%= form.label :rank_id %>
    <%= form.number_field :rank_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
