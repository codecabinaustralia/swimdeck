<%= form_with(model: competency_level, local: true) do |form| %>
  <% if competency_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(competency_level.errors.count, "error") %> prohibited this competency_level from being saved:</h2>

      <ul>
      <% competency_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

    <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
